<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    
    <%- include('partials/header') %>

    <main>
        <div class="container">
            <h1>Login</h1> <!-- user is not logged in -->
            <form action="/login" method="POST">
                <!-- Display the error message:-->
                <% if (typeof error !== 'undefined') { %>
                    <p class="error-message"><%= error %></p> 
                <% } %>
                <div class="form-group">
                    <label for="email" id="titleLabel">Email or Phone:</label>
                    <input type="text" id="email" name="email" required>
                </div>
        
                <div class="form-group">
                    <label for="password" id="titleLabel">Password:</label>
                    <input type="password" id="password" name="password" required>
                </div>
        
                <div class="buttons single-button">
                    <button type="submit">Login</button>
                </div>                
            </form>
            <p class="centered"><a href="/forgot-password">Forgot Password?</a> | <a href="/register">Register</a></p>        
        </div>
    </main>

    <%- include('partials/footer') %>

</body>

</html>